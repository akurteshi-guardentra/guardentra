# Known Issues & Follow-ups

Standalone running list of everything found-but-not-fixed while working through `docs/PRODUCT_ROADMAP_2026.md`'s sprints. Each item was discovered as a side effect of other work, deliberately not fixed at the time (out of scope, needs its own testing pass, or would have been scope creep), and should not be lost. Update this file whenever a new one is found or an existing one gets fixed — move fixed items to the "Resolved" section with the commit that fixed them rather than deleting the row.

*Last updated: 2026-08-02.*

---

## Open

| # | Issue | Where | Why it matters | Why not fixed yet |
|---|---|---|---|---|
| 2 | `src/tests/vitest.setup.ts` globally mocks `@google/genai` — now only actually exercised by `LiveAssistant.tsx`, since Sprint 0 moved the other 15 components to `fetch('/api/ai/generate')` | test config | Dead/misleading mock; `Dashboard.test.tsx`/`Risk.test.tsx` may need a `fetch` mock instead if they exercise the AI call path | Shared test-config file, didn't want to touch it blind without being able to run the suite |
| 5 | Sprint 3a's retry/promotion logic (`useOrgVendors.ts`, `useOrgAssessments.ts`) has never been executed — only reasoned through and reviewed line-by-line | `src/lib/vendor/useOrgVendors.ts`, `useOrgAssessments.ts` | Involves async Firestore listeners + timers; the riskiest code written this session | No Node/npm/Firestore emulator in this sandbox. **Needs a real pass**: via the Firestore emulator (or a dev project), go offline, create a vendor + assessment, confirm local mode, go back online, wait up to 30s, confirm they land as real Firestore docs, get removed from `localStorage`, no duplicates appear, and the retry interval actually stops once reconnected |
| 6 | Firebase ID token handling deserves a deeper look now that real non-admin members exist | `src/lib/authHeaders.ts`, `src/lib/AuthContext.tsx` | Role is read from a Firestore doc per-check (not a token custom claim) — confirm role changes take effect immediately everywhere, consider custom claims once org sizes grow, confirm no stale-token edge case when a user is removed from an org | Not urgent — quick audit already done (2026-07-25), current single-lookup model is simple and correct for today's scale; this is a "revisit later" item, not a bug |
| 11 | 11 of the 13 originally-unguarded Firestore collections (`risks`, `incidents`, `compliance`, `connectors`, `policies`, `vendor_assessments`, `executive_reports`, `calendar_events`, `identities`, `contract_audits`, `evidence_reviews`, `remediations`) still only check org-ownership, no field-shape/type validation | `firestore.rules` | Only `vendors` and `audit_readiness` (the two active-surface collections) got soft type guards in Sprint 5a | Deliberately scoped down: (a) all 11 are frozen-page-only collections today except `vendor_assessments` (no longer written at all, Sprint 3b) and `evidence_reviews`/`remediations` (AI Copilot, also frozen) — low urgency since unreachable; (b) this repo has no visibility into what's already in any deployed database, so a strict `hasOnly()` risked breaking updates to pre-existing documents with unknown fields. Revisit with real per-collection write-shape audits if/when any of these are re-enabled |
| 12 | Vendor cap enforcement (Sprint 7b) is soft, not airtight: `organizations.vendorCount` is maintained by a client-side transaction in `createVendor` (`VendorsDirectory.tsx`), and `firestore.rules` checks that counter against `vendorCap` (Starter default 25) at write time | `firestore.rules` (`orgField()`, `vendors` create rule), `src/pages/VendorsDirectory.tsx` | A client using the Firestore SDK/REST API directly (not through the app's UI) could create vendor docs while skipping the counter increment, keeping the counter artificially low and bypassing the cap indefinitely | Accepted risk, matching the same soft-metering tolerance `ARCHITECTURE_FOUNDATION.md` §4 already documents for AI usage. True hard enforcement needs vendor writes routed through a server endpoint that authoritatively counts via the Admin SDK (bigger architectural change — no live paying customers to violate a cap yet, so not attempted now) |
| 13 | Seat cap is not enforced at all — only `vendorCap`/`vendorCount` were wired in Sprint 7b | `firestore.rules`, `src/lib/orgBootstrap.ts`, `src/lib/orgInvites.ts` | Growth/Starter tier seat limits (3/10 seats per `ARCHITECTURE_FOUNDATION.md` §4) have no enforcement anywhere — an org can invite unlimited teammates today | Scoped out of Sprint 7b to avoid doubling the transaction/counter work across two different creation paths (`bootstrapUserProfile`'s new-org path and the invite-accept path) in one pass; same soft-enforcement pattern as item 12 would apply if built |
| 14 | Vitest suite showed one transient failure (`Test Files 1 failed \| 11 passed`) that did not reproduce on two subsequent clean re-runs of the exact same code | test suite generally | Likely a `waitFor`/timing-sensitive assertion in a React component test, made worse by this machine's slow/resource-constrained environment (see the multi-hour `brew install node` saga earlier this session) | Not chased further since it didn't reproduce — flagging so a future flaky-test investigation isn't starting from zero if it recurs |
| 18 | `orgs/{orgId}/vendors/{vendorId}/evidence/{fileName}` has no writer left in the app but keeps a permissive rule | `storage.rules`, `src/lib/vendor/types.ts` | Now that portal uploads go to `portal/{assessmentId}/...`, nothing writes to the `evidence/` path — but its rule is still a bare `isSignedIn()` with no org check, so any signed-in (incl. anonymous) user can still write there. `evidenceStoragePath()` in `types.ts` is now unused too | Left alone deliberately in this pass: tightening or removing the rule could break reads of files already uploaded to that path in a deployed environment, which this repo can't inspect. Fold it into the #16 deploy pass — confirm whether any objects exist under `orgs/*/vendors/*/evidence/` first, then either scope the rule to org members or drop the path entirely |
| 15 | Remediation tickets created from Audit Lab (Sprint 8b) have no view anywhere in the active app | `src/pages/AuditReadiness.tsx`, `src/components/RemediationEngine.tsx` | Tickets are real and persisted (`remediations` collection, same service the frozen legacy vendor-risk board already uses), but the only UI that renders them (`RemediationEngine`'s Kanban board) lives on `VendorRisk.tsx`, which is frozen behind the `vendorsLegacy` flag — so today a user can create a ticket but never see it again in the active product | `RemediationEngine`'s board is heavily vendor-oriented (expects a single `vendorId`/`vendorName`, "PARTNER" framing) and doesn't fit an org-wide gap list well as-is; building a proper org-wide remediation view was out of scope for this pass. `RemediationService.getAllTickets(orgId)` already exists and works if/when that view gets built |

## Resolved

| # | Issue | Fixed by |
|---|---|---|
| 3 | ✅ Vendor answers never reached the org-side review screen — `Assessments.tsx` read `q.answer` off `assessment.questions` items, but answers actually lived in a separate `answers` map keyed by question id | `141de52` — added `answers`/`comments` to `StoredAssessment` (`localAssessmentStore.ts`), normalized them through in `useOrgAssessments.ts`, and added `formatAssessmentAnswer()` + fixed the prompt builder and manual-review render in `Assessments.tsx` |
| 4 | ✅ Dead-code branch in `AuthContext.tsx`'s auto-init error handling — the `else if (initErr.code === 'permission-denied')` branch was unreachable | `141de52` — `handleFirestoreError` is now actually called for permission-denied errors (wrapped in try/catch since it always throws after logging), falling through to the local-profile fallback instead of silently skipping it |
| 7 | ✅ `vitest.setup.ts`'s `firebase/firestore` mock had no `writeBatch` export | `141de52` — added a `writeBatch` mock (`set`/`update`/`delete`/`commit`) so future tests can exercise the org-bootstrap code path |
| 9 | ✅ Hardcoded personal-email "Admin Bypass" button in `Onboarding.tsx` | `141de52` — replaced `user?.email === 'atdhee.kurteshi@gmail.com'` with `import.meta.env.DEV` |
| 10 | ✅ Frozen pages' delete buttons rendered unconditionally regardless of role | `c7707ca` — wrapped each delete control in `profile?.role === 'admin' && (...)`, matching the existing `Settings.tsx` pattern, in `Connectors.tsx`, `Policies.tsx`, `ContractNegotiator.tsx`, `VendorRisk.tsx`, `AuditCalendar.tsx`. On inspection `RiskManagement.tsx` has no delete UI at all, and `PolicyDraftsman.tsx` imports `Trash2` but never renders a delete control — so only 5 of the originally-listed 7 files actually needed the change |
| 8 | ✅ `Onboarding.tsx` unconditionally overwrote the organization's `name`/`industry`, with no check for whether the current user was the org's original creator | `1c805e6` — added `isOrgCreator = profile?.role !== 'member'` (set by `orgBootstrap.ts`'s invite-join branch) in `handleFinish()`; invited/joining members now skip the org name/industry update, framework re-initialization, and demo-data reseeding entirely — not just the name/industry write, since all three steps shared the same missing check and duplicate framework-activation/seeding on top of a live org was an equally real risk |
| 1 | ⚠️ `storage.rules` portal path now cross-references assessment `portalOpen` + org membership via `firestore.get()`/`firestore.exists()`, but **rules are not yet deployed** to any Firebase project | Code + emulator verified (`npm run test:storage-rules`, 10/10). Two follow-on bugs found during the live-readiness pass are now fixed (see #16/#17 below); remaining blockers are cloud-side only — see the deploy checklist in #16 |
| 20 | ✅ The AI model fallback for the Audit Lab (task #60) is now **verified live** against the real Gemini API | Verified 2026-08-02 on `guardentra-7f582` after the account migration. A fresh key was issued inside the project (billing account `019203-E57CB3-666105`), replacing the one revoked during the console reset. All four cases return `{"text":"OK"}` / HTTP 200: no override, `gemini-3.1-pro-preview` (what Audit Lab used to send), `gemini-1.5-flash` (VendorRisk), and a nonsense name — with exactly 3 `Ignoring unsupported model` warnings logged, one per non-default name. The earlier 429 was indeed the key living in an under-billed project, not genuine credit exhaustion. |
| 17 | ✅ **Cross-tenant portal exposure CLOSED and verified live.** A vendor holding one portal link could read and write the evidence of every other in-flight assessment, across all orgs | `9519bd3` + deployed 2026-08-02. Portal sessions now carry a server-minted `portalAssessmentId` claim (`server/routes/portal.ts`), and both rules files compare it against the assessment id in the path. Proven by `npm run smoke:portal` against the live backend in `[mode: scoped-token]`: **5/5 gating checks pass**, including the previously-failing "upload to a DIFFERENT but also-OPEN assessment's portal path" which is now `storage/unauthorized`. |
| 16 | ✅ App and rules are both deployed, in the required order | Deployed 2026-08-02 to `guardentra-7f582`. App Hosting backend `guardentra` (us-central1, nodejs24) rolled out commit `b0e3002` first, confirmed live by `/api/portal/session` minting a real token, then `firebase deploy --only firestore:rules,storage`. Required IAM: `roles/iam.serviceAccountTokenCreator` on `firebase-app-hosting-compute@…` — without it `createCustomToken()` fails and every portal link dies. |
| 19 | ✅ Rollout ordering handled; no assessments needed a `portalOpen` backfill | Both live assessments already had `portalOpen: true`, verified before the rules deploy, so nothing lost upload access. The app shipped before the rules as required — a scoped token satisfies the old rules, but the old app satisfies nothing under the new ones. |
